version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: kampanya_db
    restart: always
    environment:
      POSTGRES_USER: kampanya_user
      POSTGRES_PASSWORD: kampanya_pass
      POSTGRES_DB: kampanya_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kampanya_user -d kampanya_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    container_name: kampanya_app
    restart: always
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://kampanya_user:kampanya_pass@db:5432/kampanya_db
      PORT: 8000
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./app:/app/app

volumes:
  postgres_data:
